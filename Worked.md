# Translate_CSV2HQ_TeX_table 作業記録

## 概要
CSVの実験データ等を高品質なTeXテーブルに変換するツール「Translate_CSV2HQ_TeX_table」の初期実装を行いました。

## 実装内容
1. **環境構築 (`requirements.txt`)**
   - データ処理とTeX出力に不可欠な `pandas` および、pandasの内部でテンプレート処理に使用される `jinja2` を指定しました。

2. **CLIツールの作成 (`csv2tex.py`)**
   - `argparse` モジュールを使用し、コマンドラインから直感的に操作できるインターフェースを構築しました。
   - 入力CSVファイルパスの必須指定に加え、以下のオプションを実装しました。
     - `-o, --output`: 出力ファイルのパス指定（省略時は標準出力）
     - `-d, --digits`: 小数点以下の表示桁数を揃えるフォーマット機能
     - `-c, --caption`: テーブルのキャプション（`\caption{}`）の指定
     - `-l, --label`: テーブルのラベル（`\label{}`）の指定
     - `-s, --separator`: CSVの区切り文字の指定（タブ区切り等への対応）
     - `--na_rep`: 欠損値（NaNや空欄）を任意の文字列（デフォルトはハイフン `-`）に置換する機能

3. **高品質なTeXテーブルの出力ロジック**
   - `pandas.DataFrame.style.to_latex()` を活用し、学術論文で推奨される `booktabs` スタイル（`\toprule`, `\midrule`, `\bottomrule`）に準拠したLaTeXコードを自動生成する処理を実装しました。
   - インデックス行を非表示にし、データのみをきれいにテーブル化するよう調整しました（`hide(axis="index")`）。

4. **テストの実施**
   - サンプル用のCSVファイル (`sample.csv`) を作成し、様々なコマンドラインオプションの組み合わせで正しくTeXコードが出力されることを確認しました。

5. **ドキュメントの整備 (`README.md`)**
   - ツールの目的、必要な要件、インストール手順、および具体的なコマンドの使用例を日本語で詳細に追記しました。

## 今後の課題・拡張案
- `siunitx` パッケージ（小数点の位置を揃える `S` カラム）への対応
- より複雑なヘッダー（MultiIndexなど）を持つCSVの変換機能の強化
