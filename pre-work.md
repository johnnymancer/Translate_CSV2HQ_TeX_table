# 次の実装についての解説

## 1. 目的
現在の実装では、高品質なTeXテーブルを作成するために `booktabs` パッケージ（`\toprule`, `\midrule`, `\bottomrule`）を使用していました。しかし、ユーザーの普段の利用環境では、追加パッケージを極力使用せず、LaTeXのデフォルト機能（`\hline` など）でコンパイルできることが求められています。

そのため、出力されるLaTeXテーブルの形式を、ユーザーが提示した例のような、**縦線付きで `\hline` を使用したクラシックな形式**に変更します。

## 2. ユーザーから提示された出力例の特徴
- 配置位置: `[H]` など
- 中央揃え: `\centering`
- キャプションとラベルの付与
- 縦線を用いた列の区切り: 例 `|c||c|c|c|` のような形（`|`で囲む）
- 横線の区切り: `\hline` を使用
- `booktabs` 専用の罫線（`\toprule` 等）は**使用しない**

## 3. 実装の変更点 (`csv2tex.py`)
`pandas.DataFrame.style.to_latex()` のオプションを以下のように変更します。

- `hrules=True` を削除し、代わりに `\hline` を明示的に出力させるか、あるいはPandasでのデフォルトの罫線設定を利用します。ただし、Stylerの挙動として単純な `\hline` 出力はカスタマイズが難しい場合があるため、生成されたLaTeXの文字列に対して後処理（文字列置換など）を行うか、`column_format`（`|c|c|c|` のような指定）をユーザーが指定・または自動生成できるように工夫します。
- `position="H"` を可能にするため、必要に応じて `[htbp]` などの指定を見直します。
- 具体的な置換アプローチ：
  - `\toprule`, `\midrule`, `\bottomrule` を全て `\hline` に置換する。
  - あるいは `hrules=False` とし、自身で `\hline` を挿入する。
  - 列のフォーマット(`lrrr`など)を縦線入り(`|l|r|r|r|` や `|c|c|c|c|`)に変更するオプションの追加。デフォルトで縦線を付ける。

今回は文字列の置換と `column_format` の指定により、ユーザーが希望するクラシックな形式（`\hline`と縦線の使用）のテーブルを出力するように修正します。

---

# 今後の実装方針：multicolumn および multirow への対応

## 1. 課題と目的
表のヘッダーやデータ内でセルを結合する機能（`\multicolumn` や `\multirow`）に対応することで、階層構造を持つ複雑なCSVデータからより表現力の高い表を出力できるようにします。

## 2. 実装方針案

### A. Pandas の MultiIndex を活用するアプローチ
Pandas は `MultiIndex`（階層型インデックス）および階層型カラムを持つDataFrameを `to_latex` で出力する際、自動的に `\multicolumn` や `\multirow` を生成する機能を持っています。
- **入力側 (CSVの読み込み)**: ヘッダー行が複数あるCSVファイル（例: 1行目が大分類、2行目が小分類）を読み込む際、引数 `header=[0, 1]` などを指定して `MultiIndex` カラムを持つ DataFrame を構築します。
- **出力側 (TeXの生成)**: DataFrameの `Styler.to_latex()` を実行する際、オプション（`clines`、`sparse_index` または `sparse_columns`）を適切に設定することで、Pandas側で結合セルを生成させます。
- **メリット**: Pandasの標準機能に任せるため、独自の後処理が少なくて済みます。
- **デメリット**: 階層化の構造をCSV上でどう定義するか（例: どの行をヘッダーにするか）をCLI引数で柔軟に指定できる仕組みを作る必要があります。

### B. 特殊記号や特定文字列の置換によるアプローチ
CSVのセル内に特定のプレフィックスや記法（例: `__MC:2__Title` のように、「2列結合して"Title"を表示」）を書き込ませ、出力されたTeXコードの文字列を正規表現を用いて `\multicolumn{2}{|c|}{Title}` に置換するアプローチです。
- **入力側**: ユーザーに特殊な記法を用いてCSVを作成してもらいます。
- **出力側**: `to_latex` で生成された文字列に対して、正規表現で該当箇所を抽出し、LaTeXの結合コマンドに置き換えます。その際、結合された分だけ余る `&` 記号を削除するなどの調整が必要です。
- **メリット**: CSVの構造自体は単純な二次元配列のまま扱え、柔軟な結合が可能です。
- **デメリット**: 置換処理（特に余分な `&` の削除や罫線 `|` の整合性の確保）が複雑になる可能性があります。またユーザーに特殊な記法を強いることになります。

## 3. 次のステップ
まずは **A. Pandas の MultiIndex を活用するアプローチ** を基本として検討します。
具体的には、CLIツールに「ヘッダーとして読み込む行数」や「インデックスとして読み込む列数」を指定するオプション（例: `--header-rows 2`, `--index-cols 1`）を追加し、複数行ヘッダーのCSVを読み込んだ場合にPandasが自動生成する `\multicolumn` 等のコードが、現在の「クラシック形式（`\hline` 等を使用）」と矛盾なく出力されるかを検証しつつ実装を進めます。